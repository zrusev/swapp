import styled from 'styled-components';
import { People as EpisodePeople} from '../../../Episodes/Episode/component/_index';

export const People = styled(EpisodePeople)`
    flex-direction: column;
    align-items: center;
    margin-top: 1em;
`;